"use strict";
function makeOperation(updatedUser) {
    console.log(`name ${updatedUser.name} class ${updatedUser.email}`);
}
makeOperation({
    name: "Manikanta",
    email: "striverzarc@gmail.com",
});
